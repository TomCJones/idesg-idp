@if (ViewContext.ModelState.IsValid == false)
{
    <!-- code from identityserver4 - should be replaced by validation scripts partial -->
    <div class="alert alert-danger">
        <strong>Error</strong>
        <div asp-validation-summary="All" class="danger"></div>
    </div>
}